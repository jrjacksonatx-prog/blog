# Blog (Next.js + MDX + Dark) + Flight Deals

Run locally:
```bash
npm install
npm run dev
# open http://localhost:3000/blog
```

Deploy on Vercel:
- If your repo root is the Vercel project, set **Root Directory** to `blog` in Vercel settings.
- Build command: `npm run build`
